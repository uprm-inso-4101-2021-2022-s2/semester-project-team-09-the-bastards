import {useState} from 'react';

export const Account = () => {
    const [showPatientPortal, setShowPatientPortal] = useState(false);
    const [showMedicPortal, setShowMedicPortal] = useState(false);
}